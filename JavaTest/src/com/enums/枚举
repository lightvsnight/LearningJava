 --枚举：
    jdk1.5之后引入的一个全新的类型的 “类” 一枚举类型。为此
    jdk1.5引入了一个新的关键字 enums.我们可以这样来定义一个
    	枚举类型。
    	
          类：
      public enum Color{
         Red,
         Whiter,
         Blue
      }
      
          用法： Color MyColor = Color.Red;
          
          
     --1. 枚举类型 提供了两个实用的静态方法 values()和valueOf().
          
                        实例：
              for (Color c : Color.values()) {
                 System.out.println(c);
              }
    
     --2. 定义枚举类型时本质上就是在定义一个类别，只不过很多细节有
                        编译器帮你完成了，所以从某种程度上，enum关键字的作用就
                        像是class或interface。
                        
    
     --3. 当使用 enum 定义 枚举类型时，实质上你定义出来的枚举类型 继承自 java.lang.Enum 类型，
     	  
     	  * 每个枚举的成员其实就是你定义的枚举类型的一个实例(instance),他们都被设为final，
     	    所以你无法改变他们，他们也是static成员，所以您可以通过类型名称直接使用他们，
     	    当然最重要，他们都是公开的public。
     	    
     	  实际说：1).每个枚举的成员其实就是你定义的枚举类型的一个实例(instance),
     	       2).我们所定义的每个枚举类型都继承自java.lang.Enum类，
     	       3).枚举中的每个成员默认都是 public static final 的。
   
      --4. 每个枚举的成员其实就是你定义的枚举类型的一个实例(instance)。实例就是对象
     	   
     	   
     	   
     	   
     	   
 -- 枚举的比较
 
 -- 枚举的顺序
 
 -- 枚举的方法
     
     
     
     
     
     
     
     